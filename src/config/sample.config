# Formatting hints :
# you can put comments like in this sample (i.e. from the sharp to a new line)
# you can write multiple instructions or braces in a single line
# the data blocks (i.e. "{}" must be placed at the end of their current block)
# the route block begin with the route in a relative ou absolute path

## WEBSERV PART

user						www; # optionnal
error_log					path/to/error_log; # optionnal too
max_simultaneous_clients	666; # same

default_mime_type			string_default_mime_type;
mime_types					path/to/mime_types; # actually everything here is optionnal
index						index1 index2 index3; # it picks the first available

## WEBSERV SERVER PART

server { # server bloc
	listen				ip port;
	server_name			server_name server_name2 server_name3;

	error_page			404 path/to/404_page;
	redirection			301 path/where/redirected;
	root				path/to/root;
	autoindex			on-off;

	<loc> {
		# basically same stuff as above except for the first ones
	}
	<loc2> { redirection 304 path/to/not/modified/res; } # single line bloc
}